{
  "name": "Calil-management-app",
  "module": "index.tsx",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch index.tsx",
    "start": "bun index.tsx",
    "build:binary:linux": "bun build index.tsx --compile --target bun-linux-x64 --outfile dist/Calil-management-app-linux-x64",
    "build:binary:windows": "bun build index.tsx --compile --target bun-windows-x64 --outfile dist/Calil-management-app-win-x64.exe",
    "build:binary:mac": "bun build index.tsx --compile --target bun-darwin-x64 --outfile dist/Calil-management-app-macos-x64 && bun build index.tsx --compile --target bun-darwin-arm64 --outfile dist/Calil-management-app-macos-arm64",
    "build:binary": "mkdir -p dist && bun run build:binary:linux && bun run build:binary:windows && bun run build:binary:mac"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/puppeteer": "^7.0.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.5",
    "fast-xml-parser": "^5.3.0",
    "hono": "^4.10.3",
    "puppeteer": "^24.26.1"
  }
}
